package main

import (
	"fmt"
	"tut/ch11"
)

func main(){
	v, s := "something", "veev"
	if(word.IsPalindrome(v)){
		fmt.Println("v is pallindrome")
	}
	if(word.IsPalindrome(s)){
		fmt.Println("s is pallindrome")
	}
	
}